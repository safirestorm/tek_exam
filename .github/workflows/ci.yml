name: ci.yml

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  java-test:
    uses: ./.github/workflows/java-test.yml

  docker-ci:
    uses: ./.github/workflows/docker-ci.yml
